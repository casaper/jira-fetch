dist/
tmp/

# Secrets. A project using jira-fetch wants these two lines too.
.env*.local

# This repo's own scratch config, not a recommendation: downstream projects are meant to *commit*
# theirs, or keep one in $HOME. See .jira-fetch.example.yml.
.jira-fetch.json
.jira-fetch.yml
tmp
